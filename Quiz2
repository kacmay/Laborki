#include <stdio.h>

int main()
{
    int x;
    x = 0;
    while (x < 100);
    {
    ++x;
    if (x%3==0)
        {
        printf("x\n");
        }
    else
        {
        printf("%d\n",x);
        }
    }
    return 0;
}
